<div class="container">
    <h1>Produtos</h1>
    <ul>
        {{#each products}}
            <li>
                <h3>Nome: {{this.name}}</h3>
                <p>Preço: {{this.price}}</p>
                <form action="product/remove/{{this.id}}" method="POST">
                    <input type="submit" value="Remover">
                </form>
                <button><a href="product/{{this.id}}">Ver Produto</a></button>
            </li>
        {{/each}}
    </ul>
</div>